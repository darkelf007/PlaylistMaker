<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".domain.app.App"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        tools:targetApi="31">
        <activity
            android:name=".player.presentation.ui.PlayerActivity"
            android:exported="false"
            android:theme="@style/Theme.PlaylistMaker"/>
        <activity
            android:name=".search.presentation.ui.SearchActivity"
            android:exported="false"
            android:theme="@style/Theme.PlaylistMaker" />
        <activity
            android:name=".setttings.presentation.ui.SettingsActivity"
            android:exported="false"
            android:theme="@style/Theme.PlaylistMaker" />
        <activity
            android:name=".media.presentation.ui.MediaActivity"
            android:exported="false" />
        <activity
            android:name=".main.presentation.ui.MainActivity"
            android:exported="true"
            android:theme="@style/Main.Activity.Theme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>